# 基本方針
- chatでのuserとのやり取りは全て日本語で行うこと
- ただし、コードやスクリプト内のコメント（docstring 含む）は英語で記述すること。
- 環境構築にはuvを用いること
- 他の環境でも解析を再現できるようにuvでパッケージを管理すること
- 発言の最初に "(・ω・)/ " を示すこと
- 一つのscriptによる出力ファイルは一つのフォルダに収める。その中にサブフォルダを作成することは許可する
- 実装計画を立てる段階で決めるべき要件は　userに全てインタビューすること
- 要件をuserに尋ねる際は、同時に推奨を示すこと
- データのダウンロードなど、一時的な作業のために実行したコマンドは、LOG.mdにコマンドを記録する。その時、そのコマンドを実行した意味も一緒に記載する

# テストについて
- 動作テストを行った場合はそのscriptも保存する

# 環境構築・パッケージ管理
- Python の環境構築・依存管理には uv を使用する。pip / pip-tools / poetry / conda は使わない。
- 仮想環境作成: uv venv
- 依存追加: uv add <package>
- 依存削除: uv remove <package>
- 依存同期: uv sync
- スクリプト実行: uv run <command> （例: uv run python script.py、uv run pytest）
- ロックファイル uv.lock はコミット対象とする。

# 新しい機能を追加するとき
- 新しい機能を追加した際は README.md も更新すること（使い方・コマンド例・概要などを反映する）。

# 作業log
- 特定の GitHub issue に紐づく作業をする場合は、その issue に日本語で都度コメントを追加して作業記録を箇条書きで残すこと（`gh issue comment <番号> -b "..."` などを使う）。実施内容・決定事項・次のアクションを簡潔に記載する。
    - セッションでの実装内容・意思決定・既知の課題を簡潔に残す。
    - どの計算機・ユーザで実施したかもログに残す。
    - あるissueでの変更は、issue comment の末尾にissue番号を書いた上でcommitする
    - 作業が全て完了したら、issueの最後に報告を書いて下さい。
- さらに、作業ログを LOG.md に日付ごとに作業項目と（あれば）イシュー番号とどの計算機・ユーザで実施したかもを書く。